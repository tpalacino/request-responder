{
  "name": "@request-responder/server",
  "version": "0.0.0",
  "description": "A simple server that only serves static files from the 'public' folder.",
  "main": "server.js",
  "scripts": {
    "build": "tsc -b && shx cp -r ./public ./README.md ./dist && shx cp ./package.json.template ./dist/package.json && bestzip ./dist/server.zip ./dist/*",
    "dev": "npm run build && node ./dist/server.js"
  },
  "license": "UNLICENSED",
  "type": "module",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^24.12.2"
  }
}