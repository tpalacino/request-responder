{
  "name": "request-responder-server",
  "version": "1.0.0",
  "description": "A simple server that only serves static files from the 'public' folder.",
  "main": "server.js",
  "scripts": {
    "start": "npm i --omit=dev && node ./server.js"
  },
  "license": "UNLICENSED",
  "type": "module",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "typescript": "^5.9.3"
  }
}
